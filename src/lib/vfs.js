// NOTE: VFS implementation removed — the project currently performs direct file writes
// to keep the tool lightweight. If you need a staging/atomic commit mechanism later
// we can reintroduce a VFS implementation or a staging mode.

module.exports = null;
